import { Request, Response } from "express";

const get_favorites = (req: Request, res: Response) => {};
const update_favorites = (req: Request, res: Response) => {};

export { get_favorites, update_favorites };
